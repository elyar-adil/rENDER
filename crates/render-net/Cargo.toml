[package]
name = "render-net"
description = "Bounded HTTP transport adapter for the rENDER browser engine"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
base64 = "0.22.1"
rustls = { version = "0.23.42", default-features = false, features = ["std"] }
ureq = { version = "3.3.0", default-features = false, features = ["brotli", "gzip", "rustls"] }
url = "2.5.7"

[dev-dependencies]
flate2 = "1.1.9"

[lints]
workspace = true
