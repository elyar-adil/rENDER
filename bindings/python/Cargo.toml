[package]
name = "render-python"
description = "Python bindings for the rENDER web runtime"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[lib]
name = "_native"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.23", features = ["abi3-py310", "extension-module"] }
render-core = { path = "../../crates/render-core" }

[lints]
workspace = true
